# 开发目录 (Develop)

本目录包含小香樟心理助手项目的所有开发代码，分为客户端和服务端两个独立模块。

## 📁 目录结构

```
develop/
├── app/                # Android 客户端
│   ├── app/            # Android App模块
│   ├── build.gradle    # 项目构建配置
│   ├── settings.gradle # Gradle设置
│   ├── gradlew         # Gradle Wrapper
│   └── README.md       # 客户端文档
│
├── server/             # Spring Boot 后端
│   ├── src/            # Java源代码
│   ├── pom.xml         # Maven配置
│   ├── sql/            # 数据库脚本
│   └── README.md       # 后端文档
│
├── scripts/            # 部署/工具脚本
└── services/           # 其他微服务
```

## 🚀 快速开始

### 客户端 (Android App)

```bash
cd app
./gradlew assembleDebug
```

详细说明请参阅 [app/README.md](app/README.md)

### 服务端 (Spring Boot)

```bash
cd server
mvn -q clean package -DskipTests
java -jar target/camphor-tree-server-1.0.0.jar
```

详细说明请参阅 [server/README.md](server/README.md)

## 🔗 模块说明

| 模块 | 说明 | 技术栈 |
|------|------|--------|
| `app/` | Android 客户端，本地AI推理 | Java, llama.cpp, Material Design |
| `server/` | 后端服务，用户认证与测评 | Spring Boot, MyBatis-Plus, MySQL |
| `scripts/` | 环境配置脚本 | PowerShell/Bash |
| `services/` | 其他辅助服务 | Python, Docker |

## 📱 系统架构

```
┌─────────────────┐     ┌─────────────────┐
│   Android App   │────▶│  Spring Boot    │
│  (本地AI推理)   │     │    Server       │
└─────────────────┘     └─────────────────┘
        │                       │
        ▼                       ▼
┌─────────────────┐     ┌─────────────────┐
│   llama.cpp     │     │     MySQL       │
│   (端侧模型)    │     │   (数据存储)    │
└─────────────────┘     └─────────────────┘
```

## 👥 开发团队

- **App开发与部署**: Quan Xu, Zane Wang, Yuji Zou

