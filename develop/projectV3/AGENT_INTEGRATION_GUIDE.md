# Light Agent框架集成指南

## 📖 概述

本项目已成功集成轻量级Agent框架，将简单的本地模型调用升级为具有工具调用、多轮推理能力的智能Agent系统。

## 🎯 核心特性

### 1. ReAct推理模式
- **Reasoning (推理)**: Agent分析用户输入，判断是否需要使用工具
- **Acting (行动)**: 调用相应的工具获取信息
- **Observation (观察)**: 分析工具执行结果
- **Answer (回答)**: 基于观察结果给出最终回复

### 2. 工具系统
- **心理评估工具**: 基于对话历史评估用户心理状态
- **记忆查询工具**: 查询和检索历史对话内容
- **对话计数工具**: 管理对话轮数和评估触发

### 3. 灵活配置
- 可自定义系统提示词
- 可调节推理迭代次数
- 支持启用/禁用工具使用
- 支持启用/禁用记忆功能

## 🏗️ 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    AiChatFragment                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │              AgentManager                         │  │
│  │  ┌─────────────────────────────────────────────┐ │  │
│  │  │           AgentCore                         │ │  │
│  │  │  ┌──────────────┐  ┌──────────────────┐   │ │  │
│  │  │  │ ToolRegistry │  │  LLamaAPI        │   │ │  │
│  │  │  └──────────────┘  └──────────────────┘   │ │  │
│  │  │         │                    │             │ │  │
│  │  │         ▼                    ▼             │ │  │
│  │  │  ┌──────────────────────────────────────┐ │ │  │
│  │  │  │         ReAct Loop                   │ │ │  │
│  │  │  │  1. Thought → 2. Action →           │ │ │  │
│  │  │  │  3. Observation → 4. Answer         │ │ │  │
│  │  │  └──────────────────────────────────────┘ │ │  │
│  │  └─────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│                           │                              │
│                           ▼                              │
│  ┌───────────────────────────────────────────────────┐  │
│  │                    Tools                          │  │
│  │  ┌─────────────────┐  ┌─────────────────┐       │  │
│  │  │ Psychological   │  │  Memory         │       │  │
│  │  │ Assessment      │  │  Query          │       │  │
│  │  └─────────────────┘  └─────────────────┘       │  │
│  │  ┌─────────────────┐                            │  │
│  │  │ Conversation    │                            │  │
│  │  │ Counter         │                            │  │
│  │  └─────────────────┘                            │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 📁 文件结构

```
app/src/main/java/com/example/projectv3/
├── agent/
│   ├── Tool.java                           # 工具接口定义
│   ├── AgentConfig.java                    # Agent配置类
│   ├── ToolRegistry.java                   # 工具注册中心
│   ├── AgentCore.java                      # Agent核心推理引擎
│   ├── AgentManager.java                   # Agent管理器
│   └── tools/
│       ├── PsychologicalAssessmentTool.java  # 心理评估工具
│       ├── MemoryTool.java                   # 记忆查询工具
│       └── ConversationCounterTool.java      # 对话计数工具
└── fragment/
    └── AiChatFragment.java                 # 已集成Agent的聊天界面

app/src/main/assets/
└── agent_prompts.txt                       # Agent提示词模板
```

## 🚀 使用方法

### 1. 基本使用

在`AiChatFragment`中，Agent已经自动集成：

```java
// Agent会在模型加载后自动初始化
agentManager = new AgentManager(requireContext());
if (chatLlamaApi != null && chatLlamaApi.isModelLoaded()) {
    agentManager.initialize(chatLlamaApi);
}
```

### 2. 切换Agent模式

用户可以通过**长按发送按钮**来切换Agent模式：

- **Agent模式**: AI可以使用工具进行智能推理
- **普通模式**: AI直接回复，不使用工具

### 3. 自定义配置

如需自定义Agent配置，可以修改`AgentManager.java`中的配置：

```java
AgentConfig config = AgentConfig.builder()
    .systemPrompt("你的自定义系统提示词")
    .maxIterations(5)              // 最大推理迭代次数
    .temperature(0.7f)             // 生成温度
    .maxTokens(512)                // 最大生成token数
    .enableToolUse(true)           // 启用工具使用
    .enableMemory(true)            // 启用记忆功能
    .build();

agentManager.initialize(llmApi, config);
```

## 🔧 创建自定义工具

### 步骤1: 实现Tool接口

```java
package com.example.projectv3.agent.tools;

import com.example.projectv3.agent.Tool;
import org.json.JSONObject;

public class MyCustomTool implements Tool {
    
    @Override
    public String getName() {
        return "my_custom_tool";
    }
    
    @Override
    public String getDescription() {
        return "这是一个自定义工具，用于...";
    }
    
    @Override
    public String getParametersSchema() {
        return "{\n" +
               "  \"type\": \"object\",\n" +
               "  \"properties\": {\n" +
               "    \"param1\": {\n" +
               "      \"type\": \"string\",\n" +
               "      \"description\": \"参数1的描述\"\n" +
               "    }\n" +
               "  },\n" +
               "  \"required\": [\"param1\"]\n" +
               "}";
    }
    
    @Override
    public ToolResult execute(JSONObject parameters) {
        try {
            String param1 = parameters.getString("param1");
            
            // 执行工具逻辑
            String result = doSomething(param1);
            
            return ToolResult.success(result);
        } catch (Exception e) {
            return ToolResult.error("执行失败: " + e.getMessage());
        }
    }
    
    private String doSomething(String input) {
        // 工具的具体实现
        return "处理结果";
    }
}
```

### 步骤2: 注册工具

在`AgentManager.java`的`registerBuiltInTools()`方法中注册：

```java
private void registerBuiltInTools() {
    // 现有工具...
    
    // 注册自定义工具
    MyCustomTool customTool = new MyCustomTool();
    agentCore.registerTool(customTool);
}
```

## 📊 工具说明

### 1. 心理评估工具 (psychological_assessment)

**功能**: 基于对话历史评估用户的心理状态

**参数**:
```json
{
  "trigger_reason": "触发评估的原因"
}
```

**返回**: 包含抑郁程度、焦虑程度、风险标记和困扰分数的评估报告

**使用场景**:
- 用户表达情绪困扰
- 对话中出现心理健康关键词
- 需要了解用户当前心理状态

### 2. 记忆查询工具 (memory_query)

**功能**: 查询和检索历史对话内容

**参数**:
```json
{
  "query_type": "recent|keyword|summary",
  "keyword": "搜索关键词（可选）",
  "limit": 10
}
```

**查询类型**:
- `recent`: 查询最近的对话
- `keyword`: 根据关键词搜索
- `summary`: 生成对话摘要

**使用场景**:
- 用户询问之前的对话内容
- 需要回顾历史信息
- 生成对话统计

### 3. 对话计数工具 (conversation_counter)

**功能**: 查询和管理对话计数状态

**参数**:
```json
{
  "action": "query|reset"
}
```

**操作类型**:
- `query`: 查询当前对话计数
- `reset`: 重置计数器

**使用场景**:
- 用户询问对话进度
- 了解距离下次评估的轮数

## 🎨 提示词工程

### 系统提示词结构

```
1. 角色定义
   - 你是谁
   - 你的职责

2. 对话风格
   - 语言特点
   - 态度要求

3. 工具使用原则
   - 何时使用工具
   - 如何使用工具

4. 注意事项
   - 限制说明
   - 安全提示
```

### 工具调用格式

Agent会自动识别以下格式的工具调用：

```json
{
  "tool": "工具名称",
  "parameters": {
    "参数名": "参数值"
  }
}
```

## 🔍 调试和日志

### 启用详细日志

在`AgentCore.java`中，所有关键步骤都有日志输出：

```java
Log.d(TAG, "Agent迭代 " + iteration);
Log.d(TAG, "LLM响应: " + response);
Log.d(TAG, "检测到工具调用: " + toolName);
Log.d(TAG, "工具执行成功: " + result);
```

### 查看日志

使用Android Studio的Logcat，过滤标签：
- `AgentCore`: Agent核心推理日志
- `AgentManager`: Agent管理日志
- `ToolRegistry`: 工具注册日志
- `AiChatFragment`: 聊天界面日志

## ⚠️ 注意事项

### 1. 性能考虑

- Agent推理会增加响应时间（每次工具调用需要额外的LLM推理）
- 建议设置合理的`maxIterations`（默认5次）
- 避免在工具中执行耗时操作

### 2. 工具调用频率

- 不要过度频繁调用工具
- 心理评估建议每5-10轮对话最多1次
- 记忆查询按需使用

### 3. 错误处理

- 所有工具都应该有完善的错误处理
- 使用`ToolResult.error()`返回错误信息
- Agent会自动处理工具执行失败的情况

### 4. 模型兼容性

- 当前实现基于本地LLaMA模型
- 小模型可能难以准确识别工具调用格式
- 建议使用4B+参数的模型以获得更好的工具调用能力

## 📈 性能优化

### 1. 减少推理次数

```java
// 设置较小的maxIterations
AgentConfig config = AgentConfig.builder()
    .maxIterations(3)  // 减少到3次
    .build();
```

### 2. 优化提示词

- 使用简洁明了的工具描述
- 提供清晰的使用示例
- 避免过长的系统提示词

### 3. 缓存机制

- 工具结果可以缓存避免重复调用
- 记忆查询结果可以临时缓存

## 🔮 未来扩展

### 计划中的功能

1. **更多内置工具**
   - 情绪日记工具
   - 放松练习推荐工具
   - 专业资源查询工具

2. **工具链支持**
   - 支持多个工具的串联调用
   - 自动规划工具调用顺序

3. **流式工具调用**
   - 支持工具执行过程的实时反馈
   - 提升用户体验

4. **工具学习**
   - 根据使用效果自动调整工具调用策略
   - 个性化工具推荐

## 📞 技术支持

如有问题或建议，请联系:
- **邮箱**: 2640289029@qq.com
- **GitHub**: https://github.com/Questiony2002

## 📄 许可证

本Agent框架遵循项目的MIT许可证。

---

**最后更新**: 2025-11-17
**版本**: 1.0.0

